<%= form_with(model: [recipe, ingredient], local: true) do |form| %>
  <% if ingredient.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ingredient.errors.count, "error") %> prohibited this ingredient from being saved:</h2>

      <ul>
      <% ingredient.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :ingredient_name %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.text_field :quantity, id: :ingredient_quantity %>
  </div>

  <div class="field">
    <%= form.label :unit_of_measurement %>
    <%= form.text_field :unit_of_measurement, id: :ingredient_unit_of_measurement %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
