<%= form_with(model: [recipe, step], local: true) do |form| %>
  <% if step.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(step.errors.count, "error") %> prohibited step from being saved:</h2>

      <ul>
        <% ingredient.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
    <% end %>

    <div class="field">
      <%= form.label :details %>
      <%= form.text_area :details, id: :step_details, cols:"50", rows:"10" %>
    </div>

    <div class="actions">
      <%= form.submit %>
    </div>
<% end %>
